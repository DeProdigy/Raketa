import { StyleSheet } from 'react-native';
import Colors from './Colors';

export default top100ScreenStyles = StyleSheet.create({
  container: {
    backgroundColor: Colors.raketaBackgroundBlue,
  },
});
